# Frontend environment example
# Currently, LifeOS frontend does not require secrets.
# Keep API keys only in the backend .env (lifeos-backend/.env).

# Example variables (optional):
# VITE_APP_NAME=LifeOS
